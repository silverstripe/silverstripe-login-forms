checks:
  javascript: true
  php: true

build:
  nodes:
    analysis:
      tests:
        override:
        - php-scrutinizer-run
        - js-scrutinizer-run

  dependencies:
    override:
      - yarn && yarn install

filter:
  paths: ["src/*", "tests/*"]
